
* Add i18n support
* Separate the reverse proxy and cluster cases in mollomphp
* Add support for audio captcha's
* Add a check for edits made using the api
* Add a cache for the server list to reduce the traffic
* Incorporate PHPMollom in a clean way, instead of just copying it to our sourcedir
